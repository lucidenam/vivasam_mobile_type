<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="edu.visang.vivasam.common.mapper.CommonSmsMapper">

    <insert id="insertSmsLog" parameterType="edu.visang.vivasam.common.model.SmsLog">
        INSERT INTO SMS_LOG
        (
           	  UID
			, TITLE
			, CONTENTS
			, REG_DTTM
         )
         VALUES(
           	  #{uid}
           	, #{title}
           	, #{contents}
           	, CURRENT_TIMESTAMP()
         )
    </insert>

    <update id="updateSmsLog" parameterType="edu.visang.vivasam.common.model.SmsLog">
       UPDATE SMS_LOG
       SET
       		RESULTS = #{results}
       		, UDT_DTTM = CURRENT_TIMESTAMP()
       WHERE UID = #{uid}
    </update>

</mapper>
